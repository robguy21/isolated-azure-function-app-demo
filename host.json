{
    "version": "2.0",
    "functions": ["GetData", "PostData", "DolphinResource"],
    "logging": {
        "applicationInsights": {
            "samplingSettings": {
                "isEnabled": true,
                "excludedTypes": "Request"
            }
        }
    }
}